{
  "name": "server",
  "version": "1.0.0",
  "description": "HashNotes backend",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "postinstall": "npm install sqlite3 && npm rebuild sqlite3"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "optionalDependencies": {
    "sqlite3": "^5.1.7"
  },
  "engines": {
    "node": "16.x"
  }
}
